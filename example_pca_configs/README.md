# PCA Configuration Examples

This folder is for reusable PCA configuration examples. Keep examples generic:
use placeholder paths such as `/path/to/holo_pca` and `/path/to/apo_pca`,
and avoid project-specific dataset names or local drive paths.

Run a PCA config with:

```bash
gromacs-pca --config your_pca_config.yaml
```
